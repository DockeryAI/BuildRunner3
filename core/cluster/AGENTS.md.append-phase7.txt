## Phase 7: RuntimeRegistry Dispatch

gateway routing rule: BR3_GATEWAY=on → all model calls via
RuntimeRegistry.execute(task). Direct ollama/requests/curl to
10.0.1.* FORBIDDEN; pre-commit cluster-guard rejects it.

cache_control passthrough: task.metadata["cache_control"] forwarded
verbatim. Never strip or modify.

cost_ledger schema — 11 fields:
  ts, runtime, model, input_tokens, cache_read_tokens,
  cache_write_tokens, output_tokens, cost_usd, latency_ms,
  skill, phase
File: /srv/jimmy/ledger/cost-YYYY-Www.jsonl (weekly rotation).
Emit gated on BR3_GATEWAY=on; shim always active.
